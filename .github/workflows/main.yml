name: Phishing Detector CI

on:
  push:
    branches:
      - main
      - cursor/phishing-email-analysis-and-detection-tool-dc47
  pull_request:

jobs:
  build-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8

      - name: Lint with flake8
        run: flake8 app

      # Optional smoke test (enable if you add pytest-based tests)
      # - name: Run tests
      #   run: |
      #     pip install pytest
      #     pytest
